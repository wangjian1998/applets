<view class="item" bindtap="handleClick" data-item="{{itemData}}">
  <view>{{index}}</view>
  <view class="info">
    <view class="name">{{itemData.name}}</view>
    <view class="source">
      <image class="icon" src="/assets/images/icons/sq_icon.png"></image>
      <text>{{itemData.ar[0].name}} </text>
      <text wx:if="{{itemData.alia[0]}}" class="dots">·</text>
      <text wx:if="{{itemData.alia[0]}}">{{itemData.alia[0]}}</text>
    </view>
  </view>
</view>
